#include <iostream>
#include <classes.h>
using namespace std;

Order build_an_order(string message){
  return Order;
}

int main(){

for (int i = 0; i < 100; i++) {
  cout << "order # " + std::to_string(i) << "\n";
}

}
